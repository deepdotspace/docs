---
title: "DeepSpace SDK"
description: "Build real-time collaborative apps in one package. Auth, storage, multiplayer, payments, and zero-config deploys to your own subdomain."
---

DeepSpace is a full-stack SDK for shipping real-time apps on Cloudflare Workers. One package gives you authentication, SQLite-backed data sync, role-based permissions, presence, collaborative editing, channel-based messaging, file storage, AI chat, scheduled jobs, payments, and deploys to `<your-app>.app.space` — or your own custom domain.

```bash
npm create deepspace@latest my-app
cd my-app
npx deepspace dev
```

A new app boots in under a minute with sensible defaults: file-based routing, Tailwind v4, shadcn-style UI primitives, and a starter worker wired to a per-app Durable Object. When you're ready to ship, one command deploys to the edge.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/get-started/quickstart">
    Scaffold, run, and deploy your first app in five minutes.
  </Card>
  <Card title="Architecture" icon="diagram-project" href="/concepts/architecture">
    How the worker, Durable Objects, and platform fit together.
  </Card>
  <Card title="Client hooks" icon="react" href="/sdk-reference/client/records">
    `useQuery`, `useMutations`, `useAuth`, and the rest.
  </Card>
  <Card title="CLI" icon="terminal" href="/cli-reference/overview">
    `deepspace dev`, `deploy`, `add`, `domain`, and more.
  </Card>
</CardGroup>

## What you can build

DeepSpace is opinionated for apps where state is shared. The SDK ships the primitives so you don't have to write them.

<CardGroup cols={2}>
  <Card title="Multiplayer apps" icon="users">
    Live cursors, presence, and per-collection RBAC with no extra plumbing.
  </Card>
  <Card title="Collaborative editors" icon="pen-to-square">
    Yjs-backed text fields and shapes, synced over WebSocket.
  </Card>
  <Card title="Messaging products" icon="message">
    Channels, DMs, reactions, and read receipts as drop-in schemas.
  </Card>
  <Card title="AI applications" icon="sparkles">
    Streamed Claude, GPT, or Cerebras with tool use over your records.
  </Card>
  <Card title="SaaS with billing" icon="credit-card">
    Subscriptions, one-time products, and refunds via Stripe Checkout.
  </Card>
  <Card title="Internal tools" icon="screwdriver-wrench">
    Dashboards, trackers, and admin consoles with auth and storage built in.
  </Card>
</CardGroup>

## Two imports

Everything you need lives behind two import paths.

```tsx
// Frontend (React)
import { RecordProvider, useQuery, useMutations, useAuth } from 'deepspace'

// Worker (Cloudflare Worker)
import { RecordRoom, verifyJwt, CHANNELS_SCHEMA } from 'deepspace/worker'
```

A third entry point, `deepspace/testing`, exposes a Playwright fixture for multiplayer tests. See the [Testing guide](/guides/testing).

## How the SDK is organized

The DeepSpace SDK has three surfaces. Most apps only need the first two.

| Surface | What lives here | Import from |
|---|---|---|
| **Client** | React hooks, providers, auth UI, theming | `deepspace` |
| **Worker** | Durable Object base classes, schemas, JWT verification, AI helpers | `deepspace/worker` |
| **CLI** | Project scaffold, dev server, deploy, integration probes | `npx deepspace` |

## Where to next

<CardGroup cols={2}>
  <Card title="Get started" icon="play" href="/get-started/introduction">
    Read the introduction, then run the quickstart.
  </Card>
  <Card title="Learn the concepts" icon="book" href="/concepts/architecture">
    Workers, Durable Objects, scopes, and the data model.
  </Card>
  <Card title="Browse guides" icon="map" href="/guides/authentication">
    Task-shaped walkthroughs — auth, messaging, payments, and more.
  </Card>
  <Card title="API reference" icon="code" href="/sdk-reference/overview">
    Every export, with signatures and examples.
  </Card>
</CardGroup>
